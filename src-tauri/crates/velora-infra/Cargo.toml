[package]
name = "velora-infra"
version = "0.1.0"
edition = "2021"
description = "Default HostServices implementations (placeholder for P3)"

[lib]
name = "velora_infra"
path = "src/lib.rs"

[dependencies]
velora-core = { path = "../velora-core" }
